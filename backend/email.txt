Steps to get Gmail App Password:

Go to https://myaccount.google.com/apppasswords (must have 2FA enabled)
Select Mail as app, Other as device (name it "LAG Website")
Click Generate - copy the 16-character code
Use that code as EMAIL_PASS
Once configured, emails will be sent from lyaymhaamerica@gmail.com to each registered member with their 6-digit token.Steps to get Gmail App Password:

Go to https://myaccount.google.com/apppasswords (must have 2FA enabled)
Select Mail as app, Other as device (name it "LAG Website")
Click Generate - copy the 16-character code
Use that code as EMAIL_PASS
Once configured, emails will be sent from lyaymhaamerica@gmail.com to each registered member with their 6-digit token.

EMAIL_USER=lyaymhaamerica@gmail.com
EMAIL_PASS=your_16_char_app_password